matches:
  - trigger: ":npmUpgradeStorybook"
    replace: "npx sb@latest upgrade"
  - trigger: ":npmUpgradeSemver"
    replace: "npx npm-check-updates --reject '@storybook/*' --target semver -u --peer"
